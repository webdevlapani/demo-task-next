import { createMuiTheme } from "@mui/material";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#885FFF",
    },
    secondary: {
      main: "#33373B",
    },
  },
});

export default theme;
